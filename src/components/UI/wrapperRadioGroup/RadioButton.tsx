function RadioButton() {
  /*
  return (
    <>
      <input 
        ref={ref} 
        name={name} 
        type="radio" 
        value={value} 
        aria-label={label}
        onChange={ () => props.onChange(props.value) }
        {...props} />
      <label htmlFor={name}>Radio answer option</label>
    </>
  );
  */
 return <div></div>;
}

export { RadioButton };